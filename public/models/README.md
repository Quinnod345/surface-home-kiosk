Face recognition model files live here.

Run:

```sh
npm run download:face-models
```

Then set `faceRecognition.enabled` to `true` and add each person's
`referenceImageUrls` in `public/kiosk-config.json`.
